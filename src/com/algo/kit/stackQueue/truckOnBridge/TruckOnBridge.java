package com.algo.kit.stackQueue.truckOnBridge;

public class TruckOnBridge {
    Solution solution = new com.algo.kit.stackQueue.truckOnBridge.Solution();
    public int execute(int bridge_length, int weight, int[] truck_weights){
        return solution.solution(bridge_length, weight, truck_weights);
    }
}
